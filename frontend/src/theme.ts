import { createTheme } from '@mui/material/styles'

// Central MUI theme — override colors and typography here, not in components
export const theme = createTheme({
  palette: {
    primary: { main: '#1976d2' },
  },
})
